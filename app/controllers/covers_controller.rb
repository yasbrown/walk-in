class CoversController < ApplicationController

  def index
    # @covers = Cover.all
    # @available_covers = @covers.where()
    # raise

    # @covers = Cover.all.where(slot.start_time =)

    # @covers = []
    # covers = Cover.where(")
    # range.each do |time|
    #   @covers << covers.each do |cover|
    #     cover.seats == seats) &&
    #       cover.slots.where(start_time: time).where(available?: true).where(date: date)
    #   end
    #   @covers = @covers.flatten
    # end
  end
end
